"""Like endpoint'lari.

Endpoint'lar:
    POST /posts/{post_id}/like       — qo'yish / olib tashlash (toggle)
    GET  /posts/{post_id}/like       — holat
    POST /comments/{comment_id}/like
    GET  /comments/{comment_id}/like
"""

# TODO: router = APIRouter(tags=["likes"])
