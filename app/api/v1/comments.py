"""Izoh endpoint'lari.

Endpoint'lar:
    GET    /posts/{post_id}/comments      — post izohlari (javoblari bilan)
    POST   /posts/{post_id}/comments      — izoh qoldirish
    GET    /comments/{comment_id}
    GET    /comments/{comment_id}/replies
    PATCH  /comments/{comment_id}
    DELETE /comments/{comment_id}
"""

# TODO: router = APIRouter(tags=["comments"])
