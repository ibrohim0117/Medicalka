"""`/posts` endpoint'lari.

Endpoint'lar:
    GET    /posts             — ro'yxat (paginatsiya, qidiruv, filtr)
    POST   /posts             — yaratish
    GET    /posts/{post_id}
    PATCH  /posts/{post_id}
    DELETE /posts/{post_id}
"""

# TODO: router = APIRouter(prefix="/posts", tags=["posts"])
