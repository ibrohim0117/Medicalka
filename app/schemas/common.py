"""Paginatsiya va umumiy javob shakllari.

Bu yerda nima bo'ladi:
    * `ORMModel` — `from_attributes=True` bilan asosiy klass
    * `PaginationParams` (`page`, `size`) va `pagination_params` dependency
    * `Page[T]` — `items`, `total`, `page`, `size`, `pages`
    * `Message`, `ErrorResponse`
"""

# TODO: from pydantic import BaseModel, ConfigDict
