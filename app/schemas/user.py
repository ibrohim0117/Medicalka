"""Foydalanuvchi va autentifikatsiya shakllari.

Bu yerda nima bo'ladi:
    * `UserCreate` (parol qoidalari bilan), `UserUpdate`, `PasswordChange`
    * `UserPublic` (boshqalarga ko'rinadigan), `UserRead` (to'liq profil)
    * `LoginRequest`, `Token`, `RefreshRequest`, `TokenPayload`
    * `VerifyEmailRequest`, `ResendVerificationRequest`
"""

# TODO: from pydantic import BaseModel, EmailStr, Field
