"""Post shakllari.

Bu yerda nima bo'ladi:
    * `PostCreate`, `PostUpdate` (barcha maydonlar ixtiyoriy)
    * `PostRead` — muallif (`UserPublic`) va hisoblagichlar bilan
    * `PostWithViewerState` — lenta uchun, `is_liked_by_me` qo'shiladi
"""

# TODO: from app.schemas.user import UserPublic
